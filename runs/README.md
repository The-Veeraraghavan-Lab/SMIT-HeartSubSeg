# Runs

This directory stores local training artifacts such as checkpoints and TensorBoard event files.

The run contents are intentionally gitignored. Keep this README so the folder remains visible in the repository.
