# Pretrained Models

This directory stores pretrained weight files used by the project.

Large model binaries are intentionally gitignored. Keep this README so the folder remains visible in the repository.
